# Learning Log

각 단계가 끝날 때 자신의 말로 작성합니다.

## 단계 0

- 오늘 구현한 것:
- 새로 알게 된 개념:
- 입력과 출력:
- 발생한 오류와 원인:
- 아직 설명하기 어려운 것:

## 1차 구현 기록 — 2026-09-07 정리

아래는 코드·실행 결과·학습 대화에 근거한 정리입니다. 작성자의 이해도나 독립적인 구현 능력을 확인했다는 의미는 아닙니다. 위의 개인 기록 양식은 그대로 두었습니다.

| 단계 | 직접 실습한 대상 | 입력 → 출력 / 핵심 개념 |
|---|---|---|
| 1 | 문서·페이지·청크 데이터 모델 | 출처와 본문 정보 → Document / Page / Chunk |
| 2 | 실제 PDF 한 페이지 추출·청킹 | PDF 18페이지 → 300자·overlap 50자 청크 |
| 청킹 테스트 | 짧은 입력·경계·중복·잘못된 인자 등 | 예상 결과를 assert로 확인 |
| 4 | 문장 임베딩 | passage/query 접두어가 붙은 텍스트 → 384차원 벡터 |
| 5 | 코사인 유사도 검색 | 질문 벡터 + 청크 벡터 → 유사한 청크 순위 |
| 6 | Qdrant local | 벡터 + 문서·페이지·청크 payload → 저장·검색 |
| 7 | 전체 PDF 색인 | 텍스트가 있는 136페이지 → 747개 청크 |
| 8 | 검색 평가 | 질문과 페이지·청크 정답표 → Page / Evidence Hit@K·MRR |
| 9 | 무근거 질문과 임계값 | 양성·무근거 검색 점수 → TP/FP/TN/FN·Precision·Coverage·FAR |
| 10 | LangChain + Claude | 질문 + 검색한 context → 답변 초안과 별도 출처 목록 |
| 11 | LangGraph 고정 분기 | 질문 상태 → 고정 안내 또는 RAG → 최종 상태 |
| 12 | Streamlit | 웹 폼 입력 → 기존 workflow 호출 → 초안·출처 표시 |
| 마감 검증 | 실제 웹 동작·실패 사례 확인 | 현재 동작과 미해결 한계를 문서화 |

3단계 파일은 현재 없으며 파일 번호를 맞추기 위해 새로 만들지 않았습니다.

### 실제 시행착오

- 청크 크기를 전체 문자열 길이로 지정하면 한 청크가 생긴다는 점을 직접 비교했습니다.
- overlap은 앞 청크 끝부분을 다음 청크 앞부분에 반복하는 것임을 출력으로 확인했습니다.
- `pytest -v`는 테스트 이름을 자세히 표시하고, `-s`는 print 출력을 확인할 때 사용했습니다.
- 정답 페이지의 검색과 질문을 직접 뒷받침하는 청크의 검색을 구분했습니다.
- 무근거 질문도 높은 임베딩 유사도를 얻어 하나의 점수 임계값으로 모든 질문을 나눌 수 없음을 관찰했습니다.
- LLM 객체 대신 `ChatPromptTemplate`을 생성해 발생한 오류를 통해 프롬프트 템플릿과 모델 호출 객체를 구분했습니다.
- 부정 표현 제거 후에도 원본 `compact`를 검사하던 부분을 `risk_text` 검사로 바꾼 뒤 해당 예시의 분기를 확인했습니다.
- 화면에 출처 번호가 있거나 `ANSWER_DRAFT`가 표시된다고 근거 충분성·답변 정확성이 검증된 것은 아님을 확인했습니다.
- 개인 증상 표현을 놓치는 규칙 기반 입력 검사와, 근거 부족 뒤에도 설명을 이어가는 생성 결과를 미해결 한계로 남겼습니다.

### 현재 멈추는 지점

학습용 RAG의 검색·생성·고정 입력 분기·웹 화면까지 구현했습니다.
이후 기능을 더 붙이는 대신 [검증 기록](docs/VALIDATION.md), README, [면접 준비 자료](docs/PORTFOLIO_NOTES.md)로 1차 버전을 정리했습니다.

다음 항목은 스스로 설명해 본 뒤에만 체크합니다.

- [ ] 한 질문이 들어왔을 때 어떤 파일·함수가 어떤 순서로 실행되는지 설명한다.
- [ ] 임베딩 유사도와 정답 확률이 다른 이유를 설명한다.
- [ ] Page Hit와 Evidence Hit, 검색 평가와 생성 답변 평가의 차이를 설명한다.
- [ ] 임계값을 높였을 때 오허용과 양성 질문 누락이 어떻게 바뀌는지 설명한다.
- [ ] 규칙 기반 분기의 누락과 LLM의 불완전한 답변 보류를 실제 화면으로 설명한다.
- [ ] AI의 도움을 받은 부분과 직접 실행·비교한 부분을 구분해서 말한다.
